<?xml version="1.0"?>
<clickhouse>
    <!-- 只监听 IPv4 地址，避免 IPv6 监听失败 -->
    <!-- 注意：在 config.d 目录下的配置文件会被合并到主配置中 -->
    <!-- 这里只需要覆盖 listen_host 设置即可 -->
    <listen_host>0.0.0.0</listen_host>
</clickhouse>

